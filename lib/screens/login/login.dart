import 'package:flutter/material.dart';
import 'package:itv_manager_app/screens/login/components/body.dart';

class Login extends StatelessWidget {
  const Login({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
